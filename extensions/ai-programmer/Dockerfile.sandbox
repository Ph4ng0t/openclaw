FROM node:24-slim
RUN apt-get update && apt-get install -y --no-install-recommends libssl3 && rm -rf /var/lib/apt/lists/*
RUN npm install -g --omit=dev acpx@0.1.15 @zed-industries/codex-acp@0.10.0
WORKDIR /workspace
CMD ["tail", "-f", "/dev/null"]
